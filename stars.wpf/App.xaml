<Application x:Class="stars.wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:stars.wpf"
             xmlns:viewmodels="clr-namespace:stars.wpf.ViewModels"
             xmlns:views="clr-namespace:stars.wpf.Views"
             StartupUri="Views/LoginView.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}" x:Key="HomeView">
            <views:HomeView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:FleetViewModel}" x:Key="FleetView">
            <views:FleetView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:BuildingsViewModel}" x:Key="BuildingsView">
            <views:BuildingsView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:AdventureViewModel}" x:Key="AdventureView">
            <views:AdventureView />
        </DataTemplate>
    </Application.Resources>
</Application>
